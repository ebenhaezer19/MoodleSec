# Legacy Model Directory Notice

This directory is **legacy** for historical/training compatibility only.

Canonical runtime model directory for MoodleSec is:

`proxy/ml/models/`

Runtime modules in `proxy/ml/` are normalized to load canonical models and
emit warnings when duplicate filenames are detected here.
