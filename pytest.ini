[pytest]
markers =
    integration: mark test as integration test requiring external service (ZAP, etc.)
    unit: mark test as unit test (no external dependencies)
testpaths = ml/zap_integration/tests
python_files = test_*.py
